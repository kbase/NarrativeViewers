#
# Define basic display information
#
name     : View Interactive Heatmap
subtitle : |

tooltip  : |
    Allows users to explore an ExpressionMatrix as an interactive heatmap.

icon: kb_expr-yellow.png

screenshots :
    []

#
# Define the set of other narrative methods that should be suggested to the user.
#
suggestions :
    apps:
        related :
            []
        next :
            []
    methods:
        related :
            []
        next :
            []


#
# Configure the display and description of the parameters
#
parameters :
    param0 :
        ui-name : |
            ExpressionMatrix
        short-hint : |
           Select the ExpressionMatrix to visualize.


description : |
  <p><p>This is a visualization App to view and explore an ExpressionMatrix as a heatmap. Features are displayed as rows, and the conditions as columns. Mouse over the heatmap cells to view the expression value for a given feature and a condition.</p>

  <p>The colors in the heatmap range from white (minimum expression) to red (maximum expression) as depicted in the vertical color scale located to the right of the heatmap.</p>

technical-description : |
    View heatmap
